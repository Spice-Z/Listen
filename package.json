{
  "name": "listen",
  "private": true,
  "description": "Listen's monorepo root",
  "scripts": {
    "lint": "npm run lint --workspaces --if-present",
    "lint:fix": "npm run lint:fix --workspaces --if-present",
    "license": "npx license-checker --direct --customPath licenseFormat.json --json --out packages/app/assets/licenses.json"
  },
  "workspaces": [
    "packages/app",
    "packages/firebase/functions",
    "packages/eslint-config",
    "packages/prettier-config",
    "packages/graphql-server",
    "packages/firebase/hosting"
  ],
  "devDependencies": {
    "typescript": "^5.1.3"
  },
  "author": ""
}
